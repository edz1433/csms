Place the official CPSU seal here as: cpsu-logo.png
(square PNG, transparent background recommended, ~256x256).
The UI references asset('images/cpsu-logo.png'); if absent, brand chrome
falls back gracefully (image hidden / initial avatar shown).
